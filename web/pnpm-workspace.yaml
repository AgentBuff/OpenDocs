packages:
  - "packages/*"
  - "apps/*"

# pnpm >= 10 refuses to run dependency build scripts unless they are allowlisted.
# esbuild is the only dependency here whose install script we want to run; every
# other transitive build script stays blocked. Keep this list explicit.
onlyBuiltDependencies:
  - esbuild
